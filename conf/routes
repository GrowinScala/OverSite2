# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Addresses TEST
GET     /address/:id             controllers.ApplicationController.getAddress(id : Int)

POST    /address                 controllers.ApplicationController.postAddress()

DELETE  /address/:id             controllers.ApplicationController.deleteAddress(id : Int)


#{ "address" : "xxx@mail.com" }
