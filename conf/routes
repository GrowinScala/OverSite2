# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Addresses TEST
GET     /address/:id             controllers.AddressController.getAddress(id : Int)

POST    /address                 controllers.AddressController.postAddress()

DELETE  /address/:id             controllers.AddressController.deleteAddress(id : Int)


#{ "address" : "xxx@mail.com" }


GET     /chats                   controllers.ChatController.getChats(mailbox: String ?= "inbox")
GET     /chats/:id               controllers.ChatController.getChat(id : Int)
