# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Addresses TEST
GET     /getAddress/:id             controllers.ApplicationController.getAddress(id : Int)

POST    /postAddress                controllers.ApplicationController.postAddress()


#{ "address" : "xxx@mail.com" }
